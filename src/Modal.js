
import React from "react";
import ReactDOM from 'react-dom';

import './Ropa';



function Modal(props){
    let estado = props.number
    

    ReactDOM.createPortal(
        () => {if(estado = 1){
            console.log=("llego")}
    ;


})
}

export default Modal;
